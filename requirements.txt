# Core
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

# Vector Store (RAG)
chromadb>=0.4.0

# LLM Providers (install the one you need)
openai>=1.0.0               # for Groq (free) and OpenAI (paid) — both use this
google-generativeai>=0.5.0  # for Gemini (free)

# Testing
pytest>=7.0.0
httpx>=0.25.0
